{
  "name": "fikaai",
  "version": "0.1.0",
  "private": true,
  "description": "Healthcare access that works when connectivity doesn't.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server/index.js",
    "start": "node src/server/index.js",
    "seed": "node scripts/seed.js",
    "test": "vitest run --pool=threads --maxWorkers=1",
    "smoke:modelscope": "node scripts/smoke-modelscope.js",
    "eval:qwen": "node scripts/eval-qwen.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
